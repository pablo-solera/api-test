spring:
  security:
    jwt:
      secret: ${JWT_SECRET:mySecretKeyForJWTTokenGeneration1234567890abcdef}
      expiration: 3600000 # 1 hour in milliseconds
  application:
    name: java-api

  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

  jpa:
    hibernate:
      ddl-auto: update
    generate-ddl: true
    show-sql: true

server:
  port: 8080

# Configuración de Actuator
management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,metrics
      base-path: /actuator

  endpoint:
    health:
      show-details: always
    prometheus:
      enabled: true

  # Configuración de métricas
  metrics:
    # Tags comunes para todas las métricas
    tags:
      application: ${spring.application.name}
      environment: production

    # Distribución de percentiles
    distribution:
      percentiles-histogram:
        http.server.requests: true
  prometheus:
    metrics:
      export:
        enabled: true